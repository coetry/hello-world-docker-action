on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
    - name: Hello world action step
      id: hello
      uses: coetry/hello-world-docker-action@master
      with:
        who-to-greet: '@coetry'
    - name: Get the output time
      run: echo "The time was ${{ steps.hello.outputs.time }}"
